# Rust build output (hundreds of MB — never commit)
/target/
/src-tauri/target/

# Node
node_modules/
dist/

# Tauri generates these per-platform on build
/src-tauri/gen/schemas/windows-schema.json
/src-tauri/gen/schemas/linux-schema.json

# Screenshot harness (scripts/screenshots/capture.mjs): the dev server's log
# and Playwright's own caches/output. The PNGs it writes to docs/screenshots/
# are tracked.
/scripts/screenshots/.tmp/
/test-results/
/playwright-report/
/blob-report/
