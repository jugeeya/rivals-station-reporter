# Virtual workspace: the native Iced app and the UI-free core library that
# holds every ported piece of the Python station reporter.
[workspace]
members = ["crates/station-core", "crates/station-app"]
resolver = "2"

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
