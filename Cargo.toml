# Virtual workspace: the Tauri shell plus the Tauri-free core library that
# holds every ported piece of the Python station reporter.
[workspace]
members = ["src-tauri", "crates/station-core"]
resolver = "2"

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
